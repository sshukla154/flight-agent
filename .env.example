# .env.example — template only. Credential VARIABLE NAMES, never values.
#
# Copy to .env (already gitignored — see .gitignore) and fill in real
# secrets there. Never put a real secret in this file or in
# config/config.toml: master plan §7 is explicit that secrets come from
# env or a secret file only, never from the config file, and
# config_digest strips anything that looks like a secret before hashing
# so a committed report can never leak one either.
#
# Per DECISIONS.md D6, both adapters are interface-complete but mock-only
# ships: with these unset, Amadeus/Duffel adapters raise
# ProviderNotConfigured at runtime rather than silently falling back to
# the mock provider.

# --- Amadeus: OAuth2 client_credentials (master plan §5, "Provider abstraction") ---
# NOTE (2026-08): Amadeus for Developers' free self-service portal was
# decommissioned 2026-07-17. developers.amadeus.com now serves the
# Enterprise API Portal only -- sales-gated (amadeus.com/en/contact), not
# instant/free self-signup. There is currently no way to obtain these two
# values without a direct Amadeus B2B conversation. The adapter itself
# (providers/amadeus/) stays interface-complete regardless -- these vars
# just document what it expects, per D6's "mock-only ships" stance.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
# "test" or "production" — Amadeus uses separate hosts per environment.
AMADEUS_ENV=

# --- Duffel: static bearer + mandatory Duffel-Version header (master plan §5) ---
DUFFEL_ACCESS_TOKEN=
DUFFEL_API_VERSION=

# --- FastAPI service surface: API-key auth (master plan §8.7, Phase 8b) ---
# Required for `python -m flightagent.api.app` (or any create_app() call)
# to start at all -- app creation itself refuses to run without this set.
# Defense in depth on top of the 127.0.0.1-only bind, not a replacement
# for it (see api/app.py's own module docstring and DECISIONS.md's D22).
FLIGHTAGENT_API_KEY=
